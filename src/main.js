import { createApp } from 'vue'
// import MainPage from './mainPage.vue'
// createApp(MainPage).mount('#app')
import PlaceMain from './vue/placeInfo.vue'; // PlaceMain.vue를 가져옵니다
createApp(PlaceMain).mount('#app'); // PlaceMain을 루트 컴포넌트로 설정합니다